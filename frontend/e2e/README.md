# Tanya Quiz End-to-End Tests

Run the Angular development server and the ASP.NET Core API before running the
Playwright suite. The API should use the deterministic fake provider for local
browser tests.

```bash
npx playwright test
```
